<div id="<%= dom_id user %>">
  <p>
    <strong>Last seen at:</strong>
    <%= user.last_seen_at || user.updated_at %>
  </p>

  <p>
    <strong>Posts count:</strong>
    <%= user.posts_count %>
  </p>

</div>
